@Threaded(6)
class main {
    @Threaded (6) {
        Local Int tid = Thread.id;
        printf(tid);
    }


    @Threaded (2) {
        Local Int tid = Thread.id;
        printf(tid);
        @Threaded(3) {
            Local Int tid = Thread.id;
            printf(tid);
        }
    }
}